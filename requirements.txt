pandas
numpy
yfinance
pytest
streamlit
matplotlib
# Exact safe Arrow build for Streamlit's short-lived ScriptRunner threads.
# PyArrow 25.x can SIGSEGV in this execution model; 24.0.0 is explicitly
# verified upstream as unaffected by the Arrow/mimalloc thread bug.
pyarrow==24.0.0
